# Base URL for the backend API (no trailing slash).
# Leave empty or set to your FastAPI base URL when not using mock mode.
NEXT_PUBLIC_API_BASE_URL=

# Set to "true" to run without backend: policies in localStorage, mock Q&A responses.
# Set to "false" or omit to call real APIs (POST /api/policies/upload, GET /api/policies, POST /api/qa).
NEXT_PUBLIC_MOCK_MODE=true
